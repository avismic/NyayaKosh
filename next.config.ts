import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["placehold.co", "picsum.photos", "cdn.sanity.io"], // Add any other domains if necessary
  },
};

// next.config.js

export default nextConfig;
